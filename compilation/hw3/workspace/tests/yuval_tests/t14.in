void main(){
  int x;
}
void checkBinop(){
  int x;
  bool z;
  int y;
  byte w;
  int res;
  byte bres;
  res = x * y;
  res = x + y;
  res = x - y;
  res = x / y;
  res = y * z;
  res = y + z;
  res = x - z;
  res = x / z;
  bres = w * z;
  bres = w + z;
  bres = w - z;
  bres = w / z;
}