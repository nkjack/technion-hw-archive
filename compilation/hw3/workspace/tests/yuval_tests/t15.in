void main(){
  int x;
}
void checkBinop(){
  int x;
  int z;
  int y;
  byte w;
  bool res;
  byte bres;
  res = x * y;
  res = x + y;
  res = x - y;
  res = x / y;
  res = y * z;
  res = y + z;
  res = x - z;
  res = x / z;
  bres = w * z;
  bres = w + z;
  bres = w - z;
  bres = w / z;
}